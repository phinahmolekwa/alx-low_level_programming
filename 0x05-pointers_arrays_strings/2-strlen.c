#include "main.h"

/**
 *_strlen - lenght of a string
 *@s: A pointer to an int that will be updated
 *
 *Return: 0 Always
 */

int _strlen(char *s)
{
int c;

u = 0;

while (s[u] != '\0')
{
u++;
}
return (u);
}
